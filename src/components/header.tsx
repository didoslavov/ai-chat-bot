import Logo from "@/components/ui/logo";

function Header() {
  return (
    <header className="flex items-center border-b-2 border-primary">
      <Logo />
    </header>
  );
}

export default Header;
